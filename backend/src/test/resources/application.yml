spring:
  main:
    banner-mode: off
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5432/trenero
    driverClassName: org.postgresql.Driver
    username: username
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
