pre-commit:
  parallel: true
  jobs:
    - name: web
      root: frontend/web
      stage_fixed: true
      run: npm run biome

    - name: mobile
      root: frontend/mobile
      stage_fixed: true
      run: npm run biome

    - name: phone
      root: frontend/phone
      stage_fixed: true
      run: dart fix --apply && dart format .

    - name: backend
      root: backend
      stage_fixed: true
      run: ./gradlew spotlessApply
