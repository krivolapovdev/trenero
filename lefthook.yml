pre-commit:
  parallel: true
  jobs:
    - name: web
      root: frontend/web
      stage_fixed: true
      run: npm run biome

    - name: mobile
      root: frontend/mobile
      stage_fixed: true
      run: npm run biome

    - name: backend
      root: backend
      stage_fixed: true
      run: ./gradlew spotlessApply
